title: Country

options:
  changeStatus: false
  changeSlug: false

fields:
  id:
    type: text
  language:
    type: select
    options:
      type: query
      query: kirby.languages
      text: "{{ item.name }}"
      value: "{{ item.code }}"
  example_team_id:
    type: number
    label: The number of the Example Team

