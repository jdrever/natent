title: Country
columns:
  main:
    width: 1/1
    sections:
      fields:
        type: fields
        fields:
          language:
            type: select
            options:
              type: query
              query: kirby.languages
              text: "{{ item.name }}"
              value: "{{ item.code }}"

