title: Guide Section

columns:
  main:
    width: 2/3
    sections:
      fields:
        type: fields
        fields:
          mainContent: fields/guideContent
  sidebar:
    width: 1/3
    sections:
      pages:
        label: Sub-Sections
        type: pages
        template:
         - guide-section
         - task-team-profile
         - task-select-topic


