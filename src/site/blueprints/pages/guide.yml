title: Guide

columns:
  main:
    width: 1/2
    sections:
      fields:
        type: fields
        fields:
          description:
            type: textarea
          image:
            label: Image
            type: files
            query: page.images
          status:
            type: select
            options:
              - Discovery
              - Alpha
              - Beta
              - Live
          mainContent:
            label: Main Content
            type: blocks
            size: huge
  sidebar:
    width: 1/2
    sections:
      pages:
        label: Sections
        type: pages
        template: guide-section


