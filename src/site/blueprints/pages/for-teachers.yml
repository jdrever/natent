title: For Teachers
columns:
  main:
    width: 1/2
    sections:
      fields:
        extends: sections/core-fields
  sidebar:
    width: 1/2
    sections:
      pages:
        label: Sub-Pages
        type: pages
        template: default

